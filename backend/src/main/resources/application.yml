server:
  port: 8080
  servlet:
    context-path: /SpringBootRestApi

spring:
  data:
    mongodb:
      uri: mongodb://localhost:27017/test
      # host: localhost
      # port: 27017
      # database: test
logging:
  file: logs/application.log